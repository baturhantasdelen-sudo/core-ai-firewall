global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: nexus-quantum-guard
    metrics_path: /metrics
    static_configs:
      - targets:
          - nexus-api:8000
        labels:
          service: nexus-quantum-guard
          environment: docker-compose
